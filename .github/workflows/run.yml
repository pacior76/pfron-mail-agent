name: PFRON mail agent

on:
  workflow_dispatch:
  schedule:
    - cron: "0 8 * * *"   # codziennie 08:00 UTC

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # RĘCZNY TEST: wysyła zawsze mail testowy
      - name: Run PFRON agent (test email)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        env:
          TO_EMAIL: jacek@pacior.lap.pl
          FROM_EMAIL: pacior76@gmail.com
          FROM_NAME: PFRON Alert
          SMTP_HOST: smtp-relay.brevo.com
          SMTP_PORT: "587"
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          FORCE_TEST_EMAIL: "1"
        run: |
          python pfron_agent.py

      # HARMONOGRAM: wysyła tylko gdy są nowe ogłoszenia
      - name: Run PFRON agent (scheduled)
        if: ${{ github.event_name == 'schedule' }}
        env:
          TO_EMAIL: jacek@pacior.lap.pl
          FROM_EMAIL: pacior76@gmail.com
          FROM_NAME: PFRON Alert
          SMTP_HOST: smtp-relay.brevo.com
          SMTP_PORT: "587"
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          FORCE_TEST_EMAIL: "0"
        run: |
          python pfron_agent.py

      - name: Save state (if any)
        run: |
          if [ -f state_seen.json ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add state_seen.json
            git commit -m "Update state" || echo "No changes"
            git push || echo "Nothing to push"
          fi
